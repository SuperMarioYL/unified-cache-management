name: UCM Documentation · Check RTD build

on:
  pull_request:
    paths:
      - docs-next/**
      - .readthedocs.yaml
      - .github/workflows/docs-check.yml
  push:
    paths:
      - docs-next/**
      - .readthedocs.yaml
      - .github/workflows/docs-check.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: docs-check-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Docs · Build and contracts
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4.2.2
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5.6.0
        with:
          python-version: "3.12"
      - name: Install documentation and test dependencies
        run: python -m pip install -r docs-next/requirements-dev.txt -r docs-next/requirements-translation.txt
      - name: Test documentation and translation contracts
        run: python -m pytest -q docs-next/tests
      - name: Test installation renderer
        run: node --test docs-next/tests/install-ui.test.cjs
      - name: Test release manifest contracts
        run: >-
          python -m pytest -q
          .github/release/tests/test_release_manifest.py
          .github/release/tests/test_cleanup.py
          .github/release/tests/test_compact_workflows.py
      - name: Build both RTD language roots with real published installation data
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python docs-next/tools/site.py build --lang en --strict \
            --repository "${GITHUB_REPOSITORY}" --ref "${GITHUB_SHA}" \
            --site-url https://docs.example.invalid/en/latest/
          python docs-next/tools/site.py build --lang zh --strict \
            --repository "${GITHUB_REPOSITORY}" --ref "${GITHUB_SHA}" \
            --site-url https://docs.example.invalid/zh-cn/latest/
      - uses: actions/upload-artifact@v4.6.2
        with:
          name: ucm-docs-${{ github.sha }}
          path: docs-next/site/
          if-no-files-found: error
          retention-days: 14
