## Purpose

## Modifications 

## Test

## Documentation

If this PR changes a page listed in `docs-next/translation-required.txt`, update
the matching Chinese page before merge. Other pages may explicitly fall back to
English. For a fork PR, apply the validated patch
from the translation bot comment and push it to this same PR branch; the
required `Docs translation synchronized` check validates the actual PR HEAD.
